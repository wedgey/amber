var AMBER = AMBER || {};

AMBER.CONST = function() {
  var lat = 4.944;
  var lng = 114.928;
}

AMBER.getForecast = function(lat, lng, success) {
  $.ajax({
    url: "/weather/forecast",
    dataType: 'json',
    success: success
  });
}